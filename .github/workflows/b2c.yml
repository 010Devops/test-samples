name: B2C

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: List Folders in B2C_WEBAPP_PATH
      run: |
        Get-ChildItem -Path  D:\a\test-samples\test-samples\folder -Recurse
      shell: pwsh
